% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geomattR-package.R
\docType{package}
\name{geomattR-package}
\alias{geomattR}
\alias{geomattR-package}
\title{geomattR: Calculate Geometric Attributes of Spatial Polygons}
\description{
Calculate geometric and morphometric attributes of spatial polygons with
geodesic accuracy. Computes area, perimeter, compactness, elongation,
orientation, fractal dimension, and shape indices suitable for geospatial
analysis, urban planning, and environmental science applications.

All measurements use geodesic calculations for accuracy across different
coordinate reference systems. The package automatically handles both
geographic (lon/lat) and projected CRS appropriately.
}
\details{
\subsection{Main Functions}{
\itemize{
\item \code{\link[=calculate_geometric_attributes]{calculate_geometric_attributes()}}: Calculate metrics for one or more
polygons. Runs sequentially by default; pass a \code{cl} argument for parallel
execution.
\item \code{\link[=calculate_geometric_attributes_single]{calculate_geometric_attributes_single()}}: Core computation for a single
polygon. Optimized to avoid redundant intermediate object creation.
\item \code{\link[=calculate_geometric_attributes_parallel]{calculate_geometric_attributes_parallel()}}: Backward-compatible wrapper
that delegates to \code{calculate_geometric_attributes()}.
}
}

\subsection{Helper Functions}{
\itemize{
\item \code{\link[=get_distant_points]{get_distant_points()}}: Find most distant points on convex hull
\item \code{\link[=calc_elongation]{calc_elongation()}}: Calculate elongation ratio
\item \code{\link[=calc_extent_ew]{calc_extent_ew()}}: Calculate east-west extent
\item \code{\link[=calc_extent_ns]{calc_extent_ns()}}: Calculate north-south extent
}
}

\subsection{Geodesic Measurements}{

All calculations prioritize accuracy:
\itemize{
\item \strong{Area & Perimeter}: Use \code{terra::expanse()} with explicit
\code{transform = TRUE} for automatic geodesic calculation; perimeter
is computed on the EPSG:4326 representation
\item \strong{Distances}: Use explicit \code{method = "geo"} for geodesic calculations
\item \strong{Automatic Projection}: Non-geographic CRS are handled transparently
}
}

\subsection{Example}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(geomattR)
library(terra)

# Create sample polygon in WGS84
coords <- cbind(c(0, 0, 1, 1, 0), c(0, 1, 1, 0, 0))
polygon <- vect(coords, type = "polygon", crs = "EPSG:4326")

# Calculate all attributes (geodesic by default)
result <- calculate_geometric_attributes(polygon)

# Calculate specific metrics
result <- calculate_geometric_attributes(polygon,
  metrics = c("area", "perimeter", "compactness"))

# Parallel processing
cl <- parallel::makeCluster(2)
result <- calculate_geometric_attributes(polygon, cl = cl)
parallel::stopCluster(cl)
}\if{html}{\out{</div>}}
}
}
\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/gortegasolis/geomattR}
  \item Report bugs at \url{https://github.com/gortegasolis/geomattR/issues}
}

}
\author{
\strong{Maintainer}: Gabriel Ortega-Solis \email{g.ortega.solis@gmail.com} (\href{https://orcid.org/0000-0002-0516-5694}{ORCID})

}
\keyword{internal}
